# SQLite DB パス (デフォルトのまま変更不要。設定ファイル不要で動作します)
DATABASE_URL="file:./lily.db"

# Google Drive 同期 (任意)
# Google Cloud Console で「ウェブアプリケーション」用の OAuth クライアント ID を作成し、
# 認可済み JavaScript 生成元に http://localhost:3000 と本番 URL を登録してください。
# OAuth 同意画面のスコープに drive.appdata を含めます。
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""
