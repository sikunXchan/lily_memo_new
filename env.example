# SQLite DB パス (デフォルトのまま変更不要。設定ファイル不要で動作します)
DATABASE_URL="file:./lily.db"

# Supabase 同期 (任意)
# supabase.com で無料プロジェクトを作成し、Settings → API から取得した値を貼り付け。
# 詳細は AGENTS.md の「Supabase 同期セットアップ」を参照。
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
